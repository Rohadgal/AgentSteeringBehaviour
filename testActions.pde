public void keepInScreenLimits() {
    //int buffer = 30; // Buffer distance from the screen edge

    //if (m_position.x < buffer) {
      //  m_position.x = buffer;
    //} else if (m_position.x > width - buffer) {
      //  m_position.x = width - buffer;
    //}

    //if (m_position.y < buffer) {
      //  m_position.y = buffer;
    //} else if (m_position.y > height - buffer) {
  //      m_position.y = height - buffer;
//    }
}
